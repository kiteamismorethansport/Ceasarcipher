 //This method takes txt file and creates new encrypted file
    public static void makesEncryptedFileFromFile (Path path, int keytoencrypt ) throws IOException {
        List list = Files.readAllLines(path);
        String listofstrings = "";
        char [] encryptedChar = null;
    for (int i=0; i<list.size(); i++){
        listofstrings = list.get(i) + "\n";
    }
    char [] charofstrings = listofstrings.toCharArray();
    for (int k =0; k< charofstrings.length; k++){
        int positionOfChar = alphabet.indexOf(charofstrings[k]);
        int encryptedIndexOfChar = (keytoencrypt + positionOfChar) % 41;
        encryptedChar [k] = alphabet.charAt(encryptedIndexOfChar);

    }
    String newFile = new String(encryptedChar);
    Path path1 = Path.of("C:\\Users\\фермер\\IdeaProjects\\Ceasarcipher\\src\\main\\textencodedfile\\filikkkk.txt");
    Files.writeString(path1, newFile);
        System.out.println("СПасибо файл создан!");
    }